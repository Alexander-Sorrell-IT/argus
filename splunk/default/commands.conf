# Argus custom search commands
# forkvalidate: run fork-based exploit reproduction inside the Splunk pipeline.
#   | forkvalidate tx_hash="0x..." fork_block=<N> foundry_test="poc/replays/x/Exploit.t.sol"
# Verdict is deterministic (CONFIRMED only on a real Foundry [PASS]); no AI in the verdict path.
[forkvalidate]
filename = forkvalidate.py
chunked = true
python.version = python3
