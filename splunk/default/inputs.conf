# Argus in-app AI security agent — runs the agentic triage loop inside the
# Splunk app via the Python SDK (the "AI agents for Splunk apps" capability).
[argus_agent://main]
lookback = -180d
interval = 300
index = omni_guard_security
disabled = 0
